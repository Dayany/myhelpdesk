import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  container: {},
  spacing: {
    margin: "20px",
  },
  mainBox: {
    flexGrow: 1,
    height: "100vh",
    overflow: "auto",
  },
}));

export default useStyles;
